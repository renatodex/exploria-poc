<%= form_for(@scene_action) do |f| %>
  <% if @scene_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scene_action.errors.count, "error") %> prohibited this scene_action from being saved:</h2>

      <ul>
      <% @scene_action.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :scene_id %><br>
    <%= f.text_field :scene_id %>
  </div>
  <div class="field">
    <%= f.label :scene_callback_id %><br>
    <%= f.number_field :scene_callback_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
