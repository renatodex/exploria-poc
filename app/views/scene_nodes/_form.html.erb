<%= form_for(@scene_node) do |f| %>
  <% if @scene_node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scene_node.errors.count, "error") %> prohibited this scene_node from being saved:</h2>

      <ul>
      <% @scene_node.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scene_id %><br>
    <%= f.text_field :scene_id %>
  </div>
  <div class="field">
    <%= f.label :neighbor_scene_id %><br>
    <%= f.number_field :neighbor_scene_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
